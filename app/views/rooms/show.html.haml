%p#notice= notice

%p
%h1 
  Listing stuffs in class 
  = @room.name

%table.table
  %tr
    %th Name
    %th Description
    %th Price
    %th Type
    %th Class
    %th User
    %th Funding source
    %th
    %th
    %th

  - @stuffs.each do |stuff|
    %tr
      %td= stuff.name
      %td= stuff.description
      %td= stuff.price
      %td= stuff.type_id
      %td= stuff.room_id
      %td= stuff.user_id
      %td= stuff.funding_id

%br

= link_to ' Back', rooms_path, class: %w(glyphicon glyphicon-chevron-left)
%br
= link_to ' Download PDF', {:format => :pdf}, class: %w(glyphicon glyphicon-download-alt)
